{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "healthcheckPath": ":8080",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ALWAYS"
  }
}
